# from sqlmodel import SQLModel, Field

# class WorkerRoleLink(SQLModel, table=True):
#     worker_id: str = Field(nullable=True, foreign_key='worker.id')
#     role_id: str = Field(nullable=True, foreign_key='role.id')
